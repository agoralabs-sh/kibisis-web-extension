module.exports = {
  // global options
  artifactsDir: 'dist',
  sourceDir: '.build',

  // build options
  build: {
    overwriteDest: true,
  },

  // run options
  run: {
    firefox: '.firefox/firefox',
  },
};
