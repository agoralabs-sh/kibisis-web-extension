enum StoreNameEnum {
  Accounts = 'accounts',
  Application = 'application',
  Networks = 'networks',
  Register = 'register',
  Sessions = 'sessions',
  Settings = 'settings',
}

export default StoreNameEnum;
