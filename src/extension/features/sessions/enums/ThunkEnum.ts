enum ThunkEnum {
  RemoveAllFromStorage = 'sessions/removeAllFromStorage',
  FetchFromStorage = 'sessions/fetchFromStorage',
  RemoveAuthorizedAddress = 'sessions/removeAuthorizedAddress',
  RemoveByIdFromStorage = 'sessions/removeByIdFromStorage',
  SaveToStorage = 'sessions/saveToStorage',
}

export default ThunkEnum;
