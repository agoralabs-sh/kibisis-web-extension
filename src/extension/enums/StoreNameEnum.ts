enum StoreNameEnum {
  Accounts = 'accounts',
  Assets = 'assets',
  Messages = 'messages',
  Networks = 'networks',
  Register = 'register',
  Sessions = 'sessions',
  Settings = 'settings',
  System = 'system',
  Transactions = 'transactions',
}

export default StoreNameEnum;
