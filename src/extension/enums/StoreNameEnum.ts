enum StoreNameEnum {
  Accounts = 'accounts',
  Application = 'application',
  Assets = 'assets',
  Messages = 'messages',
  Networks = 'networks',
  Register = 'register',
  Sessions = 'sessions',
  Settings = 'settings',
}

export default StoreNameEnum;
