enum StoreNameEnum {
  Accounts = 'accounts',
  Assets = 'assets',
  Events = 'events',
  Messages = 'messages',
  Networks = 'networks',
  Register = 'register',
  SendAssets = 'send-assets',
  Sessions = 'sessions',
  Settings = 'settings',
  System = 'system',
}

export default StoreNameEnum;
