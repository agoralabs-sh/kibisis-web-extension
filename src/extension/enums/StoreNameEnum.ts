enum StoreNameEnum {
  Accounts = 'accounts',
  Assets = 'assets',
  Events = 'events',
  Messages = 'messages',
  Networks = 'networks',
  Notifications = 'notifications',
  Register = 'register',
  SendAssets = 'send-assets',
  Sessions = 'sessions',
  Settings = 'settings',
  System = 'system',
}

export default StoreNameEnum;
