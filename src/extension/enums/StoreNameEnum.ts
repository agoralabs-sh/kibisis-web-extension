enum StoreNameEnum {
  Accounts = 'accounts',
  Assets = 'assets',
  Messages = 'messages',
  Networks = 'networks',
  Register = 'register',
  Sessions = 'sessions',
  Settings = 'settings',
  System = 'system',
}

export default StoreNameEnum;
